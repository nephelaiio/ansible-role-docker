---
- name: reload docker unit
  command: systemctl daemon-reload
  become: yes
  changed_when: False
  tags:
    - skip_ansible_lint

- name: apt update
  command: apt update
  become: yes
  when: ansible_os_family == "Debian"
